# Accurate tokenization for RAG / summarization (OpenAI-compatible BPE).
# Optional: core graph build works without this file.
#
# If pip tries to compile Rust (no wheel for your Python), try:
#   - Python 3.12 or 3.13 from https://www.python.org/downloads/
#   - pip install -U pip setuptools wheel
#   - pip install tiktoken --only-binary=:all:
#
tiktoken>=0.12.0,<0.13
